- @groupmessages.each do |message|
  .message-top
    .message-top__name
      = current_user.name
    .message-top__date
      - @time = Time.new
      = @time.strftime("%Y/ %m/ %d/ %H:%M")
  .message-bottom
    = message.text
    = image_tag message.image, size:'120x180'
